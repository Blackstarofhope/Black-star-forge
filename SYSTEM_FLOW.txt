╔════════════════════════════════════════════════════════════════════════════════╗
║                    THE BLACK STAR SWEATSHOP - SYSTEM FLOW                       ║
║                         Autonomous Revenue Agent                                ║
╚════════════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────────┐
│                         1. PROJECT ORDER INTAKE                              │
└─────────────────────────────────────────────────────────────────────────────┘

    HTTP POST /api/receive-order
         ↓
    { project_name, requirements }
         ↓
    Orchestrator.receiveOrder()
         ↓
    Generate orderId (nanoid)
         ↓
    Create ProjectState
         ↓
    Store in activeProjects Map
         ↓
    Launch executeOrderAsync() [BACKGROUND]
         ↓
    Return orderId to client


┌─────────────────────────────────────────────────────────────────────────────┐
│                      2. PLANNING PHASE (30-60s)                              │
└─────────────────────────────────────────────────────────────────────────────┘

    Planner.generatePlan()
         ↓
    Send requirements to Gemini 1.5 Pro
         ↓
    ┌──────────────────────────────────────┐
    │  "Break this project into atomic     │
    │   steps: setup → features →          │
    │   integration → validation"          │
    └──────────────────────────────────────┘
         ↓
    Receive JSON plan with steps
         ↓
    Convert to PlanStep[] format
         ↓
    Save plan.md to workspace
         ↓
    Status: 'planning' → 'coding'


┌─────────────────────────────────────────────────────────────────────────────┐
│                    3. EXECUTION LOOP (5-15 minutes)                          │
└─────────────────────────────────────────────────────────────────────────────┘

    FOR EACH step in plan:
         ↓
    ┌─────────────────────────────────────┐
    │  3.1 CODE GENERATION                │
    └─────────────────────────────────────┘
         ↓
    Executor.executeStep()
         ↓
    Build context from previous steps
         ↓
    Send prompt to Gemini 1.5 Pro
         ↓
    ┌──────────────────────────────────────┐
    │  "Generate code for: [step.title]    │
    │   Description: [step.description]    │
    │   Previous code: [context]"          │
    └──────────────────────────────────────┘
         ↓
    Receive generated code
         ↓
    Clean markdown formatting
         ↓
    Save to workspace/generated/step-X.ts
         ↓
    ┌─────────────────────────────────────┐
    │  3.2 SIX EYES VALIDATION            │
    └─────────────────────────────────────┘
         ↓
    ╔═══════════════════════════════════════════╗
    ║      THE INFINITY BARRIER (Doc-Verify)   ║
    ╚═══════════════════════════════════════════╝
         ↓
    Check for deprecated patterns:
      • Stripe: 'source:', 'card:', 'bitcoin_receiver'
      • Firebase: 'firebase.database()'
         ↓
    Send code to Gemini for API analysis
         ↓
    ┌──────────────────────────────────────┐
    │  "Analyze for deprecated patterns    │
    │   in [stripe/firebase] APIs"         │
    └──────────────────────────────────────┘
         ↓
    Response: VALID / ISSUE FOUND
         ↓
    ╔═══════════════════════════════════════════╗
    ║    SPECTRAL ANALYSIS (Syntax Gate)       ║
    ╚═══════════════════════════════════════════╝
         ↓
    Run: tsc --noEmit --project workspace
         ↓
    Capture stdout/stderr
         ↓
    Check exit code === 0
         ↓
    IF errors → return detailed error log
         ↓
    ╔═══════════════════════════════════════════╗
    ║   DOMAIN EXPANSION (Visual Proof)        ║
    ╚═══════════════════════════════════════════╝
         ↓
    Launch Puppeteer (headless)
         ↓
    Navigate to localhost:3000
         ↓
    Wait for networkidle
         ↓
    Capture full-page screenshot
         ↓
    Save as visual-proof.png
         ↓
    Send screenshot to Gemini Vision
         ↓
    ┌──────────────────────────────────────┐
    │  "Analyze this screenshot:           │
    │   - Is page blank?                   │
    │   - 404/500 errors?                  │
    │   - Actual content visible?"         │
    └──────────────────────────────────────┘
         ↓
    Response: VALID / ERROR DESCRIPTION
         ↓
    ┌─────────────────────────────────────┐
    │  3.3 VALIDATION RESULT              │
    └─────────────────────────────────────┘
         ↓
    IF all Six Eyes passed:
         → step.status = 'completed'
         → consecutiveFailures = 0
         → Continue to next step
         ↓
    IF any validation failed:
         → step.retries++
         → IF step.retries < 3:
              → Retry from 3.1
         → ELSE:
              → step.status = 'failed'
              → consecutiveFailures++
              → IF consecutiveFailures >= 3:
                   → HALT EXECUTION
                   → Send error report email
                   → Status: 'failed'
                   → EXIT LOOP
              → ELSE:
                   → Continue to next step


┌─────────────────────────────────────────────────────────────────────────────┐
│              4. STRIPE AUTOMATION (if payment step detected)                 │
└─────────────────────────────────────────────────────────────────────────────┘

    Detect payment step:
      → step.title contains "payment" OR "stripe"
         ↓
    StripeAutomator.automatePaymentSetup()
         ↓
    ┌─────────────────────────────────────┐
    │  4.1 CREATE PRODUCT                 │
    └─────────────────────────────────────┘
         ↓
    stripe.products.create({
      name: project_name,
      description: requirements,
      metadata: { orderId }
    })
         ↓
    Store: projectState.stripeProductId
         ↓
    ┌─────────────────────────────────────┐
    │  4.2 CREATE PRICE                   │
    └─────────────────────────────────────┘
         ↓
    stripe.prices.create({
      product: productId,
      unit_amount: 9900, // $99 in cents
      currency: 'usd'
    })
         ↓
    Store: projectState.stripePriceId
         ↓
    ┌─────────────────────────────────────┐
    │  4.3 CREATE PAYMENT LINK            │
    └─────────────────────────────────────┘
         ↓
    stripe.paymentLinks.create({
      line_items: [{ price: priceId, quantity: 1 }],
      metadata: { orderId }
    })
         ↓
    Store: projectState.stripePaymentLink
         ↓
    ┌─────────────────────────────────────┐
    │  4.4 INJECT INTO CODE               │
    └─────────────────────────────────────┘
         ↓
    IF step.code contains HTML:
         ↓
    Search for:
      • "<!-- PAYMENT_LINK -->" comment
      • <button> with "payment" class
      • </body> tag
         ↓
    Inject:
      <a href="STRIPE_LINK">Buy Now</a>
         ↓
    Save updated code


┌─────────────────────────────────────────────────────────────────────────────┐
│                    5. APPROVAL REQUEST (Email Notification)                  │
└─────────────────────────────────────────────────────────────────────────────┘

    All steps completed successfully
         ↓
    Status: 'coding' → 'awaiting_approval'
         ↓
    Notifier.sendApprovalRequest()
         ↓
    Compose HTML email:
      ┌──────────────────────────────────────┐
      │  Subject: 🌟 [BLACK STAR] Project    │
      │           Ready for Approval         │
      │                                      │
      │  Body:                               │
      │  • Project details                   │
      │  • Requirements                      │
      │  • Implementation summary            │
      │  • Stripe payment link (TEST)        │
      │  • Screenshot (attached)             │
      │  • Approval endpoint                 │
      │  • Rejection endpoint                │
      └──────────────────────────────────────┘
         ↓
    Send via Nodemailer to ADMIN_EMAIL
         ↓
    Log: "Approval request sent"
         ↓
    ┌─────────────────────────────────────┐
    │  AGENT PAUSES - WAITING FOR HUMAN   │
    └─────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                    6. HUMAN DECISION (Human-in-the-Loop)                     │
└─────────────────────────────────────────────────────────────────────────────┘

    Admin receives email
         ↓
    Reviews:
      • Screenshot
      • Payment link
      • Implementation details
         ↓
    ╔═══════════════════════════════════════════╗
    ║        OPTION A: APPROVE                 ║
    ╚═══════════════════════════════════════════╝
         ↓
    POST /api/deploy-approval
    { orderId: "abc123" }
         ↓
    Go to Section 7
         ↓
    ╔═══════════════════════════════════════════╗
    ║        OPTION B: REJECT                  ║
    ╚═══════════════════════════════════════════╝
         ↓
    POST /api/deploy-reject
    { orderId: "abc123", reason: "..." }
         ↓
    Status: 'failed'
         ↓
    Log rejection reason
         ↓
    END


┌─────────────────────────────────────────────────────────────────────────────┐
│                    7. DEPLOYMENT (After Approval)                            │
└─────────────────────────────────────────────────────────────────────────────┘

    Orchestrator.approveDeployment(orderId)
         ↓
    Status: 'awaiting_approval' → 'deploying'
         ↓
    Executor.deployProject()
         ↓
    ┌─────────────────────────────────────┐
    │  7.1 SWITCH STRIPE TO LIVE MODE     │
    └─────────────────────────────────────┘
         ↓
    Create new StripeAutomator(isTestMode: false)
         ↓
    Use STRIPE_LIVE_KEY
         ↓
    Recreate product in LIVE mode
         ↓
    Recreate price in LIVE mode
         ↓
    Generate new payment link (LIVE)
         ↓
    Update: projectState.stripePaymentLink
         ↓
    ┌─────────────────────────────────────┐
    │  7.2 DEPLOY PROJECT                 │
    └─────────────────────────────────────┘
         ↓
    [Future: Vercel/Netlify CLI integration]
         ↓
    Generate deployment URL:
      → https://project-name.vercel.app
         ↓
    Status: 'deploying' → 'completed'
         ↓
    ┌─────────────────────────────────────┐
    │  7.3 CONFIRMATION EMAIL             │
    └─────────────────────────────────────┘
         ↓
    Notifier.sendDeploymentConfirmation()
         ↓
    Email includes:
      • ✅ Deployment success
      • 🌐 Live URL
      • 💳 LIVE payment link
      • 🎉 Celebration
         ↓
    END - PROJECT COMPLETE


┌─────────────────────────────────────────────────────────────────────────────┐
│                    ERROR HANDLING: HALLUCINATION BLOCKING                    │
└─────────────────────────────────────────────────────────────────────────────┘

    On each step failure:
         ↓
    step.retries++
         ↓
    IF step.status === 'failed':
         ↓
    Check previous step:
         ↓
    IF previousStep.status === 'failed':
         → consecutiveFailures++
    ELSE:
         → consecutiveFailures = 1
         ↓
    IF consecutiveFailures >= 3:
         ↓
    ╔═══════════════════════════════════════════╗
    ║    🛑 HALT EXECUTION                     ║
    ╚═══════════════════════════════════════════╝
         ↓
    Status: 'failed'
         ↓
    Log: "3 consecutive failures - halting"
         ↓
    ErrorHandler.createErrorReport()
         ↓
    Notifier.sendErrorReport()
         ↓
    ┌──────────────────────────────────────┐
    │  Subject: 🚨 [BLACK STAR] Project    │
    │           Failed                     │
    │                                      │
    │  Body:                               │
    │  • Error details                     │
    │  • Failure count: 3                  │
    │  • Progress summary                  │
    │  • All step statuses                 │
    │  • "Halted to prevent token burn"    │
    └──────────────────────────────────────┘
         ↓
    END - AGENT STOPPED


╔════════════════════════════════════════════════════════════════════════════╗
║                           MONITORING ENDPOINTS                              ║
╚════════════════════════════════════════════════════════════════════════════╝

    GET /api/health
      → { status: "operational" }

    GET /api/statistics
      → { total, planning, coding, awaiting_approval, completed, failed }

    GET /api/projects
      → [ { orderId, name, status, currentStep, totalSteps }, ... ]

    GET /api/project-status/:orderId
      → { orderId, name, status, plan: [...], stripePaymentLink }


╔════════════════════════════════════════════════════════════════════════════╗
║                          STATE MACHINE DIAGRAM                              ║
╚════════════════════════════════════════════════════════════════════════════╝

                    [Order Received]
                           ↓
                    ┌─────────────┐
                    │  'planning' │
                    └─────────────┘
                           ↓
                    ┌─────────────┐
                    │   'coding'  │
                    └─────────────┘
                           ↓
                    ┌─────────────┐
                    │'validating' │
                    └─────────────┘
                           ↓
            ┌──────────────┴──────────────┐
            │                             │
      [All Pass]                    [3 Failures]
            │                             │
            ↓                             ↓
    ┌───────────────┐              ┌──────────┐
    │ 'awaiting_    │              │ 'failed' │
    │  approval'    │              └──────────┘
    └───────────────┘                    ↓
            ↓                       [Error Email]
    ┌───────┴────────┐                   ↓
    │                │                  [END]
[Approve]      [Reject]
    │                │
    ↓                ↓
┌──────────┐   ┌──────────┐
│'deploying'│   │ 'failed' │
└──────────┘   └──────────┘
    ↓
┌──────────┐
│'completed'│
└──────────┘
    ↓
[Confirmation Email]
    ↓
  [END]


╔════════════════════════════════════════════════════════════════════════════╗
║                           COMPONENT INTERACTION                             ║
╚════════════════════════════════════════════════════════════════════════════╝

                         ┌──────────────────┐
                         │   Orchestrator   │  ← Main Coordinator
                         │   (Singleton)    │
                         └────────┬─────────┘
                                  │
                 ┌────────────────┼────────────────┐
                 │                │                │
                 ↓                ↓                ↓
         ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
         │   Planner    │ │   Executor   │ │   Notifier   │
         └──────────────┘ └───────┬──────┘ └──────────────┘
                 │                │
                 │        ┌───────┴───────┐
                 │        │               │
                 ↓        ↓               ↓
         ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
         │  Gemini AI   │ │  Perception  │ │    Stripe    │
         │ (Planning)   │ │    Layer     │ │  Automator   │
         └──────────────┘ └──────┬───────┘ └──────────────┘
                                  │
                         ┌────────┴────────┐
                         │                 │
                         ↓                 ↓
                 ┌──────────────┐  ┌──────────────┐
                 │  TypeScript  │  │  Puppeteer   │
                 │   Compiler   │  │  + Gemini    │
                 │              │  │    Vision    │
                 └──────────────┘  └──────────────┘


╔════════════════════════════════════════════════════════════════════════════╗
║                                THE END                                      ║
║                    Black Star Sweatshop System Flow                         ║
║                   "Autonomous Development While You Sleep"                  ║
╚════════════════════════════════════════════════════════════════════════════╝
